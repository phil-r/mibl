version: '3'
services:
  api:
    restart: unless-stopped
    build: .
    command: npm start
    # command: npm run dev
    # environment:
    # - "ENV_VAR=env_value"
    volumes:
      - ./src:/app/src
    ports:
      - 3000:3000